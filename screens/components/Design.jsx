import { View, Text } from "react-native";

export default function Design() {
  return (
     <View className={`fixed top-0 bottom-0 w-screen bg-[#333F55] opacity-30 -z-10`}>
          
     </View>
  );
}